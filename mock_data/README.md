This directory holds bundled sample data for mock mode.

Mock data files are generated by `scripts/generate_mock_data.py` and
are gitignored (they contain realistic but synthetic financial data).

Tickers covered: AAPL, JPM, JNJ, XOM, WMT
