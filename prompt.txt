Run the full evaluation suite on BOTH agent implementations using live WRDS data, then update README.md with real results.

## Steps

1. First, build the RAG transcript index in live mode:
   python main.py --index-transcripts --live

2. Run evaluation with the original agent loop (live WRDS data):
   python main.py --evaluate --live
   Save the full terminal output to artifacts/eval_original_live.txt

3. Run evaluation with the LangGraph agent loop (live WRDS data):
   python main.py --evaluate --live --langgraph
   Save the full terminal output to artifacts/eval_langgraph_live.txt

4. Update README.md:
   - Replace any evaluation metrics that were based on mock data with the real live WRDS results
   - In the "## LangGraph Implementation" section, add a comparison table showing both versions' results side by side (tool sequence diversity, RAG utilization, token usage, total iterations)
   - Make it clear these metrics are from live WRDS data, not mock
   - Keep the mock-mode instructions in README for credential-free demo purposes

5. Commit: git add -A && git commit -m "docs: update README with live WRDS evaluation results for both agent versions"

## Notes
- .env has WRDS_USERNAME and ANTHROPIC_API_KEY already configured
- If the RAG index build fails on live transcripts, fall back to --mock for indexing and note this in README
- If any single ticker fails during evaluation (e.g. missing CIQ transcript data), skip it and continue with remaining tickers — note which tickers succeeded
- Do NOT modify any source code in this step — only run evaluations and update README